<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Fnio.UI.Controls">

    <Style TargetType="controls:ImageLoader">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:ImageLoader">
                    <Grid>
                        <Image x:Name="ImageContainer" Stretch="{TemplateBinding Stretch}"/>
                        <ContentPresenter x:Name="IndicatorPresenter" Content="{TemplateBinding Indicator}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
